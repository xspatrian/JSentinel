/*!/wp-content/themes/salient-child/assets/footer-older.js*/
jQuery(function($){$('.page-template-default .accordion-section').each(function(){const section=$(this);const accordions=section.find('.toggles.accordion .toggle.default');const images=section.find('.accordion-images .img-with-aniamtion-wrap, .accordion-images .wpb_video_widget');images.first().addClass('active');accordions.each(function(key){$(this).attr('data-tab',key);if(images.eq(key).length){images.eq(key).attr('data-tab',key)}});accordions.on('click',function(){let clickedAccordion=$(this);let tab=clickedAccordion.attr('data-tab');if(!clickedAccordion.hasClass('open')){setTimeout(function(){images.removeClass('active');let targetImage=images.filter('[data-tab="'+tab+'"]');let imgVideo=targetImage.find('img, video');let imageHeight=imgVideo.outerHeight();let imagesContainer=section.find('.accordion-images');let accordionContent=section.find('.accordion-content');let containerHeight=accordionContent.height();let relativeTop=clickedAccordion.position().top;imagesContainer.css('padding-bottom',(relativeTop+'px'));targetImage.css('top',relativeTop+'px');targetImage.addClass('active')},300)}})})})
;